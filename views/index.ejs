<% include ./partials/header %>

<h1>Index Page</h1>

<% blogs.forEach(function(blog){ %>
    <h2><%= blog.title %></h2>
    <img src="<%= blog.image %>">
    <p><%= blog.body %></p>
    <p><%= blog.date %></p>
<% }) %>

<% include ./partials/footer %>
